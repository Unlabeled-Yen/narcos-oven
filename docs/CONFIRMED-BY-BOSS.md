# 雇主 confirm 記錄

_日期：2026-07-02_
_對象：NARCOS.sugar 老闆_
_訪談人：Yen（AI 導入 builder）_
_用途：這是 pipeline 邏輯的 source of truth。任何實作分歧優先看此檔。_

---

## 1. c22（配送數量）= 2 → 出幾張標籤？

**答**：**每箱一張標籤**。

**Pipeline 影響**：
- Stage 5 (Recipient Grouping) 產出的 `labelCount` 直接 = c22
- 標籤編號規則：一單 c22=2 → 「2-1」「2-2」；c22=3 → 「3-1」「3-2」「3-3」
- 面交/KOL 沒有 c22 概念，用**同批次同收件人的品項數** = 標籤數

---

## 2. 面交 c17「焙茶巴斯克 $980」 vs 賣貨便「焙茶栗子巴斯克」

**答**：**本來就有兩種品項，各別對應不同價格**。以菜單為準（[fixtures/2026-07-round1/菜單/訂購方式_20211016_無麩貼文-2.jpg]）：

| 品項 | 價格 |
|---|---|
| 原味巴斯克 | $650 |
| 芝麻巴斯克 | $720 |
| **純焙茶巴斯克** | **$880**（新獨立 SKU） |
| **焙茶栗子巴斯克** | **$980**（不是 $880！） |
| 白玉烏龍茶巴斯克 | $980 |
| 升級白玉夾餡 | +$100（可用於原味/芝麻） |

**menu.yaml 已 lock**：新增 `焙茶巴斯克` SKU；把 `焙茶栗子巴斯克` 價由 $880 改成 $980。

**⚠️ 重要修正 (2026-07-02 深入 review 後發現)**：兩份資料源的品項名字都跟菜單不一致，我改成「信價格不信名字」邏輯：

| 資料源 | 名字 | 售價 | 對映 SKU（依價格） |
|---|---|---|---|
| 賣貨便 c12 | 「無麩 ✦ 焙茶栗子巴斯克 六寸 焙茶巴斯克（純焙茶，不添加栗子）」 | **$880** | → `焙茶巴斯克`（純焙茶）$880 ← 因括號註「不添加栗子」 |
| 面交 c17 header | 「焙茶巴斯克 $980」 | **$980** | → `焙茶栗子巴斯克` $980 ← 面交簡稱把「栗子」略掉 |

**判定邏輯**：**售價為 anchor**。名字混淆時看價格。這反映在 menu.yaml 的 `match_signature` 設計。

**⚠️ 待再問雇主**：
- 賣貨便後台的 SKU 命名分類是否要整理（把 $880 純焙茶從「焙茶栗子」大分類移出）？
- 面交表 c17 是否要改成「焙茶栗子巴斯克 $980」避免誤導？

---

## 3. 未付款訂單（c5 = 「訂單成立（尚未付款）」）是否進本批出爐量？

**答**：**未付款不納入出爐量，付款確認才排程**。

**Pipeline 影響**：
- **Stage 1 (Parse) 新增 filter**：`c5 !== "付款完成..."` → 進 `pending_payment` 桶，**不進主軌**
- 儀表板要有「等待付款訂單」列表，數字獨立顯示（雇主可追進度）
- 客人一旦付款、後端有機制標記 `paid: true` → 該筆重跑 pipeline 進出爐排程

**待補問**：雇主怎麼標記「已付款」？（賣貨便匯出的隔天就會自動更新 c5、還是要雇主手動改？）

---

## 4. 客人備註（c28）藏日期指示 vs c12 選項不一致

**答**：**客人備註是重要的、但出爐排程需要後續經 AI 設計流程、要再討論**。

**Pipeline 影響（v1 保守做法）**：
- Stage 4 (Batch Assignment) 檢查 c28 是否 regex 中日期樣式
- 若 c28 含日期且 c12 也有指定出貨日、且**兩者不同** → 進 `pending_conflict_date` 桶（新 code），標「c12 選 X、c28 寫 Y」
- 若 c28 含日期但 c12 沒選 → 進 `pending_batch_date`，AI 建議用 c28 日期、Yen 拍板
- v2 目標：AI 幫排程（雇主要與 Yen 再討論怎麼設計）

---

## 5. 賣貨便 c17 運費（買家付的）是否算 NARCOS 營收？

**答**：**錢到店家、店家再付給物流**——**運費算營收**，但需另扣「實際物流成本」算淨營收。

**Pipeline 影響**：
- 總營收 = c21 (含運費) ✅
- 淨營收 = c21 - 品項成本 - 包材 - **實際物流費**
- menu.yaml 或 config 加一格：**物流成本表**（超商取貨 $60、宅配 $130 等實際店家付出金額）
- v1 用「每單物流估 $100」預設，雇主之後補精確金額

---

## 6. KOL 未完成 sheet 進度狀態機

**答**：**已寄出 (c6 = True) 就判定為已完成**。

**Pipeline 影響**：
- KOL parser 讀「未完成」sheet 時，`c6 === True` → **視為完成，不進本批出爐量**（等同「已完成」sheet 語意）
- `c6 === False` + `c4 (寄貨時間) === 本批日期` → 進本批出爐量
- `c6 === False` + `c4` 是模糊字串（如「七月台中面交」）→ 進 `pending_batch_date`
- **不再看** c0 (折扣碼設置完成) 欄——那是 Yen 內部管理欄，不影響出爐

---

## 7. KOL 品項字串 alias 對應是否正確？

**答**：**對應都正確**。日後表單會依 SKU 統一化。

**KOL「擇一」品項**：**由 KOL 自行選擇**（她會告訴我們，或看 IG 貼文）。

**Pipeline 影響**：
- menu.yaml v1 的 aliases 段已收錄 KOL 口語名（`◆ 四入肉桂捲` → `經典肉桂捲4入` 等）
- **擇一類**（例如「一盒：肉桂捲+磅蛋糕擇一」）→ 進 `pending_kol_choice` 桶
  - 每筆待 Yen 手動填「KOL 實際選了哪一款」
  - 或未來 AI 讀 KOL 訊息自動填（v2）
- 「擇一」不進出爐量、直到解決

---

## 8. 出貨標籤實體規格

**答**：**A. 熱感應標籤機**。

**⚠️ 待補問（尺寸細節）**：
- 標籤紙寬度？（40mm / 50mm / 60mm / 100mm）
- 每張標籤高度？（常見 30mm / 40mm / 60mm）
- 標籤機型號？（Brother QL-8xx / TSC / Zebra ...）
- 機器介面：USB / 藍牙 / 網路？
- 或用你目前 jpg 的比例（看起來單張 ~60mm × 90mm 左右）？

**v1 預設**：60mm × 90mm 縱向、單張一片、產出 PDF 給熱感應機驅動吃。你之後給機型再改。

---

## 9. 「澎湖駐店私定」「park2 蛋糕活動」「6/27 之前客人下訂」歸類

**答**：**丟到「待分類」桶由雇主自行分類**。

**Pipeline 影響**：
- 面交 c2 若不 match 任何 `(面交|冷凍宅配|宅配)` 樣式 → 進 `pending_channel` 桶
- 待處理桶 UX 提供「新增通路」按鈕，讓雇主自訂通路名（例：「駐店」「活動」「私定」變成 first-class channel）

---

## 10. `長型4入_兩磅` SKU 的 display_name 尾巴被 Excel 截斷

**答**：**權威原文只需要符合中文字**。

**這是最大架構影響**——原本以為 display_name 要跟賣貨便 c12 百分百一致（含 `✦` `✧` 空白），現在改為：

**新 lookup 演算法**：
1. **先跟 `aliases[]` 做 exact 字串 match**（100% 命中最快最準）
2. 若無命中，跟 `match_signature` 做**中文字子串 include/exclude 判定**
3. `display_name` 純粹是 UI 顯示用（中文精簡版）

**menu.yaml schema 新增**：
```yaml
match_signature:
  include: [中文字1, 中文字2, ...]   # 全部必包含
  exclude: [中文字3, ...]            # 全部不能包含
```

例：
- `原味巴斯克`: `include: [原味巴斯克], exclude: [白玉, 焙茶, 芝麻]`
- `白玉原味巴斯克`: `include: [白玉, 原味巴斯克]`

---

## 📋 第二輪 confirm（2026-07-02）

### R2-1. 焙茶栗子舊訂單 $880 vs 菜單 $980
**答**：已解決（見上 #2「重要修正」段——alias 依售價 anchor 重綁定）。

---

### R2-2. 未付款訂單「已付款」的標記機制

**答**：**付款機制會是「人工重新匯出報表 → AI 或程式工作流判斷 + 更新」**。

**Pipeline 影響（重要架構）**：
- SQLite `orders` 表需 `UNIQUE(order_id)` constraint
- 每次匯入賣貨便 xlsx 走 **upsert 流程**：
  ```
  for each row in new xlsx:
      if order_id 已存在:
          比對 c5 狀態
          若舊=「訂單成立」而新=「付款完成」→ 該 order 從 pending_payment 移到 confirmed，重跑 Stage 2-7
          若狀態沒變 → skip
      else:
          正常走主軌
  ```
- **憲章防護 #1 總數守恆律** 要多一條：同一 order_id 在跨批 upsert 中前後計數必一致

**MCP tool 相關**：新增 `refresh_payment_status(new_xlsx_path)` 讓 Claude Code 觸發「只跑付款 reconciliation、不跑其他」

---

### R2-3. 熱感應標籤機規格

**答**：**標籤檔案就是尺寸**（用你既有的 jpg 作為 spec）。

**實際量到的**：
- 每張 JPG：**590 × 945 px**
- 內含 **3 個標籤直排**（虛線分隔）
- 單一標籤約 590 × 315 px（aspect 1.87:1）
- 無 DPI 元資料（手機截圖式）

**Pipeline 影響**：
- Labels PDF 產出的每頁 = 590×945 等效比例（用向量繪製、印表機決定實體 mm）
- 若印表機需要精確 mm，取 200 DPI 換算 → 75×120mm 每張 JPG（單標籤 75×40mm）
- 待雇主日後告知機型再 lock 精確 DPI

---

### R2-4. 物流實際成本

**答**：**晚點給你**。

**Pipeline 影響**：
- menu.yaml `logistics_cost:` 段先用 v1 預設值（超商 $60、宅配 $130）
- 雇主給值後 Yen 手改該段、下批分潤自動用新值
- 憲章原則：**不用 LLM 猜、直接用雇主給的表**

---

### R2-5. 面交/KOL 標籤數（無 c22 欄位）

**答**：**該筆品項數量 = 標籤數**。

**Pipeline 影響**：
- Stage 5 Recipient Grouping 判定：
  ```
  if channel == 賣貨便: labelCount = c22
  elif channel in (面交, KOL, 宅配, 待分類): labelCount = sum(item.quantity for item in items)
  ```
- 這與雇主 confirm #1 一致（每箱一張）
- 例子：面交一筆點「肉桂捲四入 x1 + 芝麻巴斯克 x1」→ 2 張標籤（2-1, 2-2）

---

## 📋 尚未 confirm（待第三輪）

1. **物流實際成本表**（雇主 R2-4 說晚點給）
2. **熱感應機型**（雇主 R2-3 說看 jpg 就好、但若要精確 mm 需知型號）
3. **賣貨便 SKU 命名整理**（是否要把 $880 純焙茶從「焙茶栗子」大分類移出？）
4. **面交 c17 header 是否改為「焙茶栗子巴斯克 $980」避免誤導？
